# recursive-download 

This script allows the recursive downloading of files in a specific directory and its sub-directories. 
It requires the filesystem extension to be loaded! 

![preview](./images/preview.png)
